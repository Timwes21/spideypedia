const base = "http://localhost:3000";
const comicsBase = base + "/comics";
const authBase = base + "/auth";
const submitToAgentApi = base + "/agent/convo";

export {comicsBase, authBase, submitToAgentApi}

